# ADR 0001：运行时不使用堆

状态：Accepted

STM32F103C8T6 只有 20 KiB SRAM。长期运行的游戏固件不能接受碎片化、分配失败路径和不可预测分配时间。因此 FreeRTOS 只开启静态分配，不编译 heap 实现；任务、队列、画布、字符串、游戏身体数组和模块表均有编译期容量。

代价是容量必须显式预算，队列满需要确定策略，功能上限在编译期固定。收益是内存占用可由 map 文件审计，启动后不会因堆状态变化产生新的失败模式。
