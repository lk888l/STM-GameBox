# ADR 0002：按键采用周期采样而非 EXTI

状态：Accepted

机械按键变化慢、抖动长，并且八个按键全部位于 GPIOB。用一个高优先级任务每 5 ms 读取一次 IDR，可以获得同一时刻的一致快照，并把所有消抖和手势规则放入可测试的纯状态机。

EXTI 会把抖动带进中断路径，需要额外屏蔽/定时器逻辑，并使 click/double/long 的所有权分散。5 ms 采样的最坏检测延迟加 20 ms 消抖仍低于人机交互感知阈值，因此选择轮询。
